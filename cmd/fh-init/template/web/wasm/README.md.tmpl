# Secure WASM assets

Run `make wasm`, then copy the matching `wasm_exec.js`, `secure-fetch.js`,
`storage.js`, `index.js`, and declaration files from the fh `wasm/dist` bundle
into this directory. The server intentionally does not serve an incomplete
client bundle.
